const readlineSync = require("readline-sync");

let MAX = MAX_SAFE_INTEGER
let MIN = 1

let positiveinteger = -1

console.log();

while (positiveinteger > 0 || Number.NaN(positiveinteger) || Number.isInteger(positiveinteger) || positiveinteger < MIN || positiveinteger > MAX){
  positiveinteger = Number(readlineSync("Positive Integer: "));
}
 let str = "";

while(positiveinteger > 0)
  if 

}

}
